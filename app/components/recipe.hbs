<article class="recipe">
    <LinkTo @route="recipe" @model={{@recipe.id}} class="button">{{@recipe.title}}</LinkTo> <button {{on "click" (fn this.deleteRecipe @recipe)}}>Supprimer</button>
</article>